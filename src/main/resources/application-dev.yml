spring:
  datasource:
    url: jdbc:mysql://localhost:4040/sfg_dev
    username: sfg_dev_user
    password: amin
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: true